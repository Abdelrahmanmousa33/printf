#include <limits.h>
#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char *stest = 0;

	_printf("%s\n", stest);
	printf("%s\n", stest);
	return (0);
}
